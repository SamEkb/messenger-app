# App settings
APP_NAME=UsersService
DEBUG=false

# HTTP (gRPC-Gateway) server settings
HTTP_HOST=0.0.0.0
HTTP_PORT=8004

# gRPC server settings
GRPC_HOST=0.0.0.0
GRPC_PORT=9004

# Kafka settings
KAFKA_BROKERS=host.docker.internal:9092
KAFKA_TOPIC=user-events
KAFKA_CONSUMER_GROUP=users-service
KAFKA_MAX_RETRY=3
KAFKA_RETRY_INTERVAL=5s
